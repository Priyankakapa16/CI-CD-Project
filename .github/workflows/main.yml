name: Deploy Static Website

on:
  push:
    branches: [ "main" ]  # main branch lo changes push chesina appudu run avuthundi

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 16

      - name: Validate HTML
        run: |
          echo "🔍 Checking HTML file..."
          ls

      - name: Upload artifact (Website)
        uses: actions/upload-artifact@v4
        with:
          name: website
          path: .

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checko
